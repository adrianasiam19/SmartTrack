# Core framework
fastapi==0.115.6
uvicorn[standard]==0.34.0

# Database (async)
sqlalchemy>=2.0.0
psycopg[binary]>=3.2.0
alembic==1.14.0

# Auth & Security
passlib[bcrypt]==1.7.4
python-jose[cryptography]==3.3.0
python-multipart==0.0.20

# Google OAuth
authlib==1.3.2
httpx==0.28.1

# Settings & Environment
pydantic>=2.11.0
pydantic-settings>=2.7.0
python-dotenv==1.0.1

# Utilities
email-validator==2.2.0
pypdf>=5.0.0

# ML alternate career model (optional at runtime if ML_ALTERNATE_ENABLED=false)
# Pin close to training version (1.8.x) to avoid pickle version warnings.
scikit-learn>=1.8.0,<1.9.0
joblib>=1.3.0
pandas>=2.0.0
numpy>=1.26.0

# AI & LLM (DeepSeek uses OpenAI-compatible API via httpx — no extra SDK needed)
